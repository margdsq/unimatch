/**
 * Next.js config used by local `next dev/build` and by Vercel.
 * Keep this file at the repo root so Vercel detects the Next.js framework
 * instead of the leftover Vite/vinext Cloudflare toolchain.
 */
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {};

export default nextConfig;
